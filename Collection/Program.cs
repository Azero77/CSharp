namespace Collection
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //Array ==> all elements are the same type
            ///Array is a refrence type;
            ///Array has a fixed space and all elements are set to null before initializing(or to zero if it was value types)
            int[,] a = new int[10, 5];
            //fill the array
            



        }
    }
}
